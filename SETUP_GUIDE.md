# 📦 دليل إعداد تطبيق إزالة الخلفية

## 🎯 ما الذي ستحصل عليه؟

ملف `BackgroundRemoverAI.exe` واحد يمكنك تشغيله على أي جهاز Windows بدون الحاجة لـ:

- ❌ تثبيت Python
- ❌ تثبيت أي مكتبات
- ❌ أي إعدادات معقدة

## 📋 متطلبات النظام

- **نظام التشغيل:** Windows 10 أو 11 (64-bit)
- **الذاكرة:** 4GB RAM (يُفضل 8GB)
- **الإنترنت:** مطلوب في أول تشغيل فقط (لتحميل الموديل)

---

## 🔧 الخطوة 1: بناء ملف الـ EXE (على جهازك الحالي)

### المتطلبات

- Python 3.9 أو أحدث
- جميع مكتبات المشروع مثبتة

### خطوات البناء

1. افتح **PowerShell** أو **Command Prompt**

2. انتقل لمجلد المشروع:

```powershell
cd "f:\دروس وافكار\دورات\دورات css\My work\مشاريع ذكاء اصطناعى\bg-remover"
```

3. شغّل سكريبت البناء:

```powershell
python build_exe.py
```

4. انتظر حتى اكتمال البناء (قد يستغرق 5-10 دقائق)

5. ستجد الملف التنفيذي في:

```
dist\BackgroundRemoverAI.exe
```

---

## 📤 الخطوة 2: نقل الملف للجهاز الجديد

انسخ **فقط** هذا الملف:

```
BackgroundRemoverAI.exe
```

---

## 🚀 الخطوة 3: التشغيل

1. **اضغط مرتين** على `BackgroundRemoverAI.exe`
2. سيفتح **المتصفح تلقائياً** على عنوان `http://localhost:8000`
3. **لن تظهر أي نافذة سوداء** (CMD مخفية)
4. استخدم التطبيق بشكل طبيعي!

### أول تشغيل

> ⚠️ في أول مرة يتم التشغيل، سيقوم التطبيق بتحميل موديل الذكاء الاصطناعي تلقائياً (حوالي 176 MB). هذا يحدث مرة واحدة فقط.

---

## 🛑 إيقاف التطبيق

### الطريقة 1: من الموقع (مُفضّلة)

- اضغط على زر **"إغلاق السيرفر"** الموجود في أسفل الصفحة

### الطريقة 2: من Task Manager

1. اضغط `Ctrl + Shift + Esc`
2. ابحث عن `BackgroundRemoverAI.exe`
3. اضغط **End Task**

---

## ⚠️ ملاحظات مهمة

| المشكلة                      | الحل                                         |
| ---------------------------- | -------------------------------------------- |
| **Antivirus يحظر الملف**     | أضف استثناء للملف في برنامج مكافحة الفيروسات |
| **المتصفح لا يفتح تلقائياً** | افتح يدوياً: `http://localhost:8000`         |
| **الملف كبير جداً**          | طبيعي! الملف يتضمن Python ومكتبات AI         |
| **بطء في أول معالجة**        | طبيعي! الموديل يُحمّل في الذاكرة             |

---

## 📊 معلومات تقنية

- **حجم الملف:** 200-500 MB
- **البورت المستخدم:** 8000
- **مجلد الملفات المؤقتة:** `temp` (بجانب الـ EXE)
- **مدة حفظ الصور:** ساعة واحدة ثم تُحذف تلقائياً
